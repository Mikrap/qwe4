<Page x:Class="Demo.Pages.EditClientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Demo.Pages"
      mc:Ignorable="d"  Background="Snow"
      d:DesignHeight="450" d:DesignWidth="400"
      Title="EditClientPage">

    <Grid>
        <StackPanel Margin="20">
            <Label Content="Full Name:"/>
            <TextBox Text="{Binding ClientFullName}" Margin="0,0,0,10"/>

            <Label Content="Address:"/>
            <TextBox Text="{Binding Address}" Margin="0,0,0,10"/>

            <Label Content="Passport:"/>
            <TextBox Text="{Binding Password}" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Click="Save_Click" Width="80" Margin="0,0,10,0"/>
                <Button Content="Cancel" Click="Cancel_Click" Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
